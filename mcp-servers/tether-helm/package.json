{
  "name": "tether-helm-mcp",
  "version": "0.0.1",
  "private": true,
  "description": "MCP server wired into Helm-enabled Tether sessions. Bridges Claude Code tool calls (spawn_session, etc.) to Tether's main process via a per-session named pipe.",
  "main": "dist/index.js",
  "bin": {
    "tether-helm-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
