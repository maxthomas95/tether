# SonarCloud configuration.
# Picked up by SonarCloud's automatic analysis on each PR.

sonar.projectKey=maxthomas95_tether
sonar.organization=maxthomas95

# Theme palettes are inherently structurally similar (5 themes × same shape).
# CPD flags this as duplication, but it's a known false positive for theme
# files — the repetition is the feature, not an accident. Exclude only this
# file from copy-paste detection; all other quality checks still apply.
sonar.cpd.exclusions=src/renderer/styles/themes.ts
